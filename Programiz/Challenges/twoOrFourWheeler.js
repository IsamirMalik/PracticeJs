// return 'two wheeler' if wheels is equal to 2 or 'Four Wheeler' if wheels is equal to 4 , else return "Invalid".

function checkVehicleType(wheels) {

    return ( wheels ==2) ? "Two-wheeler" :
         (wheels ==4 ) ? 'Four-wheeler' : "Invalid"
 
 }